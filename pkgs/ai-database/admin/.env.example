# Required - generate with openssl rand -base64 32
PAYLOAD_SECRET=replace_with_a_secure_secret

# Optional - MongoDB or PostgreSQL connection string
# DATABASE_URI=mongodb://localhost/ai-database
# DATABASE_URI=postgresql://postgres:postgres@localhost:5432/ai-database

# If no DATABASE_URI is provided, a MongoDB memory server will be used
