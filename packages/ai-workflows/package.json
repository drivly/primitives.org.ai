{
  "name": "ai-workflows",
  "version": "0.1.0",
  "description": "Lightweight wrapper around AI functionality with future extensibility for durable execution and retries",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/openai": "^1.3.21"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "tsup": "^8.0.2",
    "typescript": "^5.4.2"
  }
}
